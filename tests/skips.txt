# tests/skips.txt — Vendored CPython 3.14.6 Test Skips
#
# Tests listed here are skipped when running the vendored CPython test suite.
# Skip matching walks the MRO — PathTest.test_foo also skips
# WindowsPathTest.test_foo and PosixPathTest.test_foo.
#
# Remove entries as features are fixed in pathlibrs.
# Only private-API tests should remain as permanent skips.
#
# Stats: 43 active entries | 764 passed | 440 skipped | 0 failures
#        30 private-API entries (permanent)
#        13 platform-specific / deferred
#        (196 entries resolved from 239 baseline)
#
# ═══════════════════════════════════════════════════════════════════════

# ── Private API: _delete() (26 entries, permanent) ────────────────────
PathSubclassTest.test_delete_dir
PathSubclassTest.test_delete_does_not_choke_on_failing_lstat
PathSubclassTest.test_delete_file
PathSubclassTest.test_delete_inner_symlink
PathSubclassTest.test_delete_missing
PathSubclassTest.test_delete_on_named_pipe
PathSubclassTest.test_delete_symlink
PathSubclassTest.test_delete_unwritable
PathTest.test_delete_dir
PathTest.test_delete_does_not_choke_on_failing_lstat
PathTest.test_delete_file
PathTest.test_delete_inner_symlink
PathTest.test_delete_missing
PathTest.test_delete_on_named_pipe
PathTest.test_delete_symlink
PathTest.test_delete_unwritable
PosixPathTest.test_delete_dir
PosixPathTest.test_delete_does_not_choke_on_failing_lstat
PosixPathTest.test_delete_file
PosixPathTest.test_delete_inner_symlink
PosixPathTest.test_delete_missing
PosixPathTest.test_delete_on_named_pipe
PosixPathTest.test_delete_symlink
PosixPathTest.test_delete_unwritable
# Additional Windows-only delete entries:
PathTest.test_delete_inner_junction
PathTest.test_delete_outer_junction

# ── Private API: Unpickling 3.13 (1 entry, permanent) ─────────────────
# CPython 3.13 pickle format references pathlib._local.PurePosixPath
PurePathTest.test_concrete_class

# ── NotImplemented / UnsupportedOperation — RESOLVED (0 entries) ─────

# ── copy(): preserve_metadata not implemented (0 entries resolved) ─────

# ── copy(): permission / error handling (0 entries resolved) ────────────

# ── symlinks: complex (3 entries, Windows-only) ──────────────────────
# POSIX passes; Windows has //?/D: prefix mismatch
PathTest.test_complex_symlinks_absolute
PathTest.test_complex_symlinks_relative
PathTest.test_complex_symlinks_relative_dot_dot

# ── resolve() edge cases (2 entries, Windows-only) ───────────────────
# POSIX passes; Windows has //?/D: prefix mismatch
PathTest.test_resolve_common
PathTest.test_resolve_dot

# ── cross-flavour: equivalences — RESOLVED (0 entries) ────────────────────

# ── cross-flavour: different parsers unordered — RESOLVED (0 entries) ─

# ── ordering: cross-flavour TypeError — RESOLVED (0 entries) ────────────────────

# ── expanduser_windows — RESOLVED (0 entries) ────────────────────
# EnvironmentVarGuard.unset() multi-arg shimmed in conftest.py for Py3.10+

# ── owner/group windows (2 entries) — PENDING ────────────────────
# Need to raise UnsupportedOperation on Windows-flavoured paths.
PathTest.test_group_windows
PathTest.test_owner_windows

# ── is_reserved() DeprecationWarning — RESOLVED (0 entries) ──────────

# ── is_junction_true — RESOLVED (0 entries) ────────────────────────
# Delegates through parser.isjunction; passes on Python 3.12+ where posixpath.isjunction exists.
# On Python 3.10, the test cannot run because posixpath lacks isjunction.

# ── relative_to / is_relative_to — RESOLVED (0 entries) ──────────────

# ── mkdir(parents=True) edge case (1 entry) ──────────────────────────
# umask fix is Unix-only; Windows has different mkdir behaviour
PathTest.test_mkdir_parents

# ── rmdir (1 entry, Windows handle leak) ──────────────────────────
PathTest.test_rmdir

# ── walk() edge cases (1 entry) ───────────────────────────────────────
# walk_above_recursion_limit: infinite_recursion() fails on Python 3.10
# Windows (cannot set recursion limit to 40 at depth 42). Passes on
# POSIX and Python 3.14.
PathWalkTest.test_walk_above_recursion_limit

# ── from_uri / pathname2url POSIX — RESOLVED (0 entries) ─────────────
# pathname2url(add_scheme=True) shimmed in conftest.py for Python < 3.14.
# from_uri_posix: hostname + file:rel/path edge cases fixed.
#
# ── from_uri / pathname2url WINDOWS (2 entries, Windows-only) ────────
# Windows from_uri: UNC paths, pipe notation, localhost not implemented.
PathTest.test_from_uri_pathname2url_windows
PathTest.test_from_uri_windows

# ── CompatiblePathTest — RESOLVED (0 entries) ─────────────────────────

# ── Concrete class move edge cases — RESOLVED (0 entries) ────────────

# ── Windows-specific: parser / drive / naming — RESOLVED (0 entries) ──
# Pure-path Windows tests pass with --windows-flavour flag.
# Concrete-path Windows tests (PathTest) are gated by @needs_windows decorator.
# test_absolute_windows kept below — requires OS-specific features.

# ── test_absolute_windows (1 entry, Windows-only) ───────────────────
# Uses os.chdir() and os_helper.subst_drive() — cannot run on POSIX.
PathTest.test_absolute_windows

# ── Constructor / nesting — RESOLVED (0 entries) ────────────────────

# ── fspath / is_absolute pure path — RESOLVED (0 entries) ────────────

# ── Misc: descriptor / junction / docstrings (6 entries) ─────────────
PathSubclassTest.test_matches_writablepath_docstrings
PathTest.test_matches_writablepath_docstrings
PosixPathTest.test_matches_writablepath_docstrings

# ── Compat: truediv/rtruediv — RESOLVED (0 entries) ──────────────────

# ── kwargs passthrough — RESOLVED (0 entries) ──────────────
# ── docstrings — RESOLVED (0 entries) ──────────────────────────
