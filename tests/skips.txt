# tests/skips.txt — Vendored CPython 3.14.6 Test Skips
#
# Stats: 2 active entries | 810 passed | 394 skipped | 0 failures (macOS)
#        2 blocked (PyO3 / Windows chmod)
#       (237 entries resolved from 239 baseline)

# ── test_concrete_class — PyO3 #[new] cannot return subclass ──────────
PurePathTest.test_concrete_class

# ── test_delete_unwritable — Windows chmod cannot prevent file deletion
PathTest.test_delete_unwritable
